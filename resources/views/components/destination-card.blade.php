<div class="service-item bg-green-50 wow fadeInUp rounded-2xl shadow-sm hover:shadow-md fix-underline"
    data-wow-delay="0.1s">

    {{ $photo }}
    {{-- <img class="absolute img-fluid w-100 h-100 max-w-full max-h-full" src="{{ asset('storage/' . $feature->image) }}"
alt="{{ $feature->title }}" style="object-fit: cover;" /> --}}
    <div class="p-6">
        {{ $content }}
    </div>
</div>
